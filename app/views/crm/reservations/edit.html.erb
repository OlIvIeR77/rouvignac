
<h1 style="text-align:center;padding-top:1em;">Update reservation</h1>
<%= form_for @reservation, url: { action: 'update', controller: 'crm/reservations' } do |f| %>
  <%= render 'form', f: f %>
<% end %>
